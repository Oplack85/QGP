
services:
  - type: web
    plan: free
    name: GPT
    env: python
    buildCommand: pip install -U -r requirements.txt
    startCommand: python3 ser.py & python3 main.py 
    region: frankfurt
    envVars:
      - key: ALIVE_NAME
        sync: false
      - key: APP_ID 
        sync: false
